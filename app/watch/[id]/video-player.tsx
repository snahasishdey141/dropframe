'use client'

import { Download, Play, AlertCircle } from 'lucide-react'

export function VideoPlayer({ id, pathname, expired }: { id: string; pathname: string; expired?: boolean }) {
  return <main className="flex min-h-screen items-center justify-center bg-[#0d1117] px-5 py-10 text-white"><div className="w-full max-w-4xl"><div className="mb-6 flex items-center gap-3"><div className="grid size-9 place-items-center rounded-xl bg-[#b8f36b] text-[#0d1117]"><Play className="size-4 fill-current" /></div><span className="font-mono text-sm font-bold tracking-tight">DROPFRAME</span></div><div className="overflow-hidden rounded-2xl border border-white/10 bg-black shadow-2xl">{pathname ? <video className="aspect-video w-full" controls autoPlay src={`/api/video/${id}?pathname=${encodeURIComponent(pathname)}`} /> : <div className="flex aspect-video flex-col items-center justify-center gap-4 text-center text-white/50"><AlertCircle className="size-8" /><p className="text-sm">{expired ? 'This shared link has expired.' : 'This shared link is missing its video reference.'}</p></div>}</div><div className="mt-5 flex items-center justify-between"><div><h1 className="font-medium">Shared video</h1><p className="mt-1 text-sm text-white/45">Watch in your browser · Downloads enabled</p></div>{pathname && <a className="inline-flex items-center gap-2 rounded-xl bg-[#b8f36b] px-4 py-2.5 text-sm font-semibold text-[#0d1117]" href={`/api/video/${id}?pathname=${encodeURIComponent(pathname)}&download=1`}><Download className="size-4" /> Download</a>}</div>{!pathname && <p className="mt-5 text-sm text-red-300">This link is missing its video reference.</p>}</div></main>
}
